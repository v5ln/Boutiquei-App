<?xml version="1.0" encoding="UTF-8" ?>
<StackLayout
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="Boutiquei.Views.AddressCard"
    xmlns:models="clr-namespace:Boutiquei.Models"
    x:DataType="models:Address">
    <Frame BackgroundColor="#F5F5F5" Margin="10,10,20,20">
        <StackLayout>
            <Label Text="{Binding Name}" FontSize="18" TextColor="#242424" FontFamily="NSB"></Label>
            <StackLayout Orientation="Horizontal">
                <Label Text="{Binding City}" TextColor="#808080" FontSize="14" FontFamily="NSR"></Label>
                <Label Text="{Binding AddressDetails}" TextColor="#808080" FontSize="14" FontFamily="NSR"></Label>
                
            </StackLayout>
            <StackLayout Orientation="Horizontal">
                <Label Text="{Binding District}" TextColor="#808080" FontSize="14" FontFamily="NSR"></Label>
                <Label Text="{Binding Phone}" TextColor="#808080" FontSize="14" FontFamily="NSR"></Label>
            </StackLayout>
        </StackLayout>
    </Frame>
    
   
</StackLayout>
